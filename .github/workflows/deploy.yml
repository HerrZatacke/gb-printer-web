name: Build and Deploy

on:
  push:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.1

      - name: Setup node 12
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'

      - name: Install npm dependencies
        run: npm install

      - name: Create config.json
        env:
          CONF_JSON_FILE: ${{ secrets.configjson }}
          CONF_JSON: ${{ secrets.CONFIG_JSON }}
          TEST_VAR: '{"TestVar":true}'
        run: |
          echo $CONF_JSON_FILE
          echo $CONF_JSON
          echo $TESTVAR
          echo $TESTVAR > config.json
          pwd
          ls
          cat config.json

      - name: Build
        run: npm run build

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@4.1.4
        with:
          branch: gh-pages
          folder: dist
