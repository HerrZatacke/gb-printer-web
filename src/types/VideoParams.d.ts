
export interface VideoParams {
  imageSelection?: string[],
  cropFrame?: boolean,
  exportFrameMode?: string,
  frame?: string,
  frameRate?: number,
  invertPalette?: boolean,
  lockFrame?: boolean,
  palette?: string,
  reverse?: boolean,
  scaleFactor?: number,
  yoyo?: boolean,
}
